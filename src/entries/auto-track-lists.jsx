import React from "react"
import ReactDOM from "react-dom/client"
import AutoTrack from "../views/auto-track-lists.jsx"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AutoTrack />
  </React.StrictMode>
)
