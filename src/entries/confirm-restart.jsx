import React from "react"
import ReactDOM from "react-dom/client"
import ConfirmRestart from "../views/confirm-restart.jsx"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ConfirmRestart />
  </React.StrictMode>
)
