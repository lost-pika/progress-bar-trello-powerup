import React from "react"
import ReactDOM from "react-dom/client"
import AutoTrack from "../views/card-detail-progress.jsx"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AutoTrack />
  </React.StrictMode>
)
