import React from "react"
import ReactDOM from "react-dom/client"
import CardProgress from "../views/card-progress.jsx"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CardProgress />
  </React.StrictMode>
)
